{
	"nodes":[
		{"id":"af551b8507daf58b","type":"text","text":"## Title\nAIBrix: Towards Scalable, Cost-Effective Large Language Model Inference Infrastructure\n## Abstract\n我们介绍了AIBrix，一个云原生、开源框架，旨在优化和简化云环境中大规模LLM的部署。 与传统的云原生架构不同，AIBrix遵循协同设计理念，确保基础设施的每一层都是为了与vLLM等推理引擎无缝集成而专门构建的。\n\nAIBrix引入了多项关键创新，以降低推理成本并提高性能，包括用于动态适配器调度的 高密度LoRA管理、LLM专用自动缩放器以及前缀感知、负载感知路由。 为了进一步提高效率，AIBrix集成了一个分布式KV缓存，提高了节点间的符元重用率，从而使吞吐量提高了50%，推理延迟降低了70%。 AIBrix还支持统一的AI运行时，简化了模型管理，同时保持了与厂商无关的引擎兼容性。\n\n对于大规模多节点推理，AIBrix采用混合编排——利用Kubernetes进行粗粒度调度，利用Ray进行细粒度执行——以平衡效率和灵活性。 此外，一个以SLO驱动的GPU优化器会动态调整资源分配，优化异构服务以最大限度地提高成本效率，同时保持服务保证。 最后，AIBrix通过AI加速器诊断工具增强了系统可靠性，实现了自动化故障检测和模拟测试，从而提高了容错能力。 AIBrix可在https://github.com/vllm-project/aibrix获取。\n## Summary\n基于kubernetes的构建的first架构\n## Links\nhttps://yiyibooks.cn/arxiv/2504.03648v1/index.html","x":-1240,"y":80,"width":920,"height":580},
		{"id":"fb7e3ac6ac5a89ce","type":"text","text":"# LoRA\n低秩微调（LoRA）是一种旨在通过引入低秩矩阵来减少大型语言模型微调过程中可训练参数量的参数高效微调技术 。LoRA 的核心思想是，预训练模型虽然参数众多，但其内在维度可能较低，针对特定任务的调整只需要在模型权重矩阵的低秩子空间中进行 。\n## 基本原理\nLoRA 的**基本原理**如下 ：  \n1. **冻结预训练模型权重** (W0​)：在微调过程中，原始预训练模型的权重参数保持不变，不进行更新 。这保证了模型原有的通用知识不会丢失 。  \n2. **引入低秩矩阵** (A 和 B)：对于预训练模型中的每个需要调整的权重矩阵（通常是 Transformer 架构中的注意力层和前馈网络中的线性层），LoRA 并行地引入两个小的低秩矩阵 A 和 B。假设原始权重矩阵 W0​ 的维度是 d×k，那么引入的低秩矩阵 A 的维度是 d×r，矩阵 B 的维度是 r×k，其中 r≪min(d,k)，r 被称为秩（rank），是一个远小于原始矩阵维度的超参数 。  \n3. **低秩分解近似权重更新** (ΔW)：LoRA 将全秩的权重更新 ΔW 近似为两个低秩矩阵的乘积：ΔW≈BA 。由于 r 远小于 d 和 k，因此矩阵 A 和 B 的参数量远小于原始权重矩阵 W0​ 和潜在的全秩更新 ΔW。  \n4. **仅训练低秩矩阵**：在微调过程中，只有低秩矩阵 A 和 B 中的参数会被训练和更新，而原始的预训练权重 W0​ 保持冻结 。这大大减少了需要学习的参数数量。例如，对于 GPT-3 这样的模型，使用 LoRA 可以将可训练参数的数量减少 10000 倍 。  \n5. **推理时的权重合并**：在模型推理阶段，可以将训练好的低秩矩阵的乘积 BA 加回到原始的权重矩阵 W0​ 中，得到更新后的权重 W=W0​+BA 。这样，LoRA 在推理时不会引入额外的计算开销，与直接使用微调后的全参数模型速度相当 。","x":-80,"y":-640,"width":820,"height":460},
		{"id":"0e3a7e2dab35ec83","type":"file","file":"Agent/Asserts/20250501164655816.png","x":-40,"y":200,"width":1398,"height":1140}
	],
	"edges":[]
}